# Dependencies
node_modules/

# Test coverage (Vitest, etc.)
coverage/

# Environment and secrets
.env
.env.*
*.key
*.pem

# Runtime config (contains real API keys)
openclaw.json

# SQLite databases
*.db
*.db-wal
*.db-shm
*.sqlite

# LanceDB data
lancedb/

# Vendored copies (e.g. Anthropic skill-creator reference tree)
third-party/

# Internal / maintainer-only docs (npm publish setup, etc.)
internal/

# OS files
.DS_Store
Thumbs.db
.claude/settings.json
.claude/worktrees/
extensions/memory-hybrid/dist/

# Accidental workspace writes under extension dir (invalid OPENCLAW_WORKSPACE path)
extensions/memory-hybrid/undefined/

extensions/memory-hybrid/npm-shrinkwrap.json

# Auto-generated memory index (created at runtime in workspace root)
MEMORY_INDEX.md
extensions/memory-hybrid/MEMORY_INDEX.md
